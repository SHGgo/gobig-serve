package top.gobig.mapper;

public class Test {
/*    List<Video> selectByUid(@Param("uid") Integer uid, @Param("videoNum")Integer videoNum);

    List<Video> selectBySortName(@Param("sortName") String sortName, @Param("videoNum")Integer videoNum);

    <select id="selectByUid" resultType="Video">
    select *
    from video v
    where v.uid = #{uid}
    limit #{videoNum}
  </select>

  <select id="selectBySortName" resultType="Video">
    select *
    from video v
    where v.sort_name = #{sortName}
    limit #{videoNum}
  </select>


  int selectTotalByVid(GDao dao);

  List<Comment> selectByVid(GDao dao);

  */
}
